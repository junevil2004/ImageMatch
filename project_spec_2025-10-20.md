# 이미지 유사도 검색 프로젝트 기술 명세서 (2025-10-20)

## 1. 프로젝트 목표
사용자가 업로드한 이미지와 가장 유사한 이미지를, 기존에 저장된 이미지들 중에서 찾아 순서대로 보여주는 웹 애플리케이션입니다.

## 2. 시스템 아키텍처
- **프론트엔드 (Frontend):** 사용자가 직접 상호작용하는 웹 화면 부분입니다. (React)
- **백엔드 (Backend):** 이미지 처리, AI 모델 연산, 데이터 관리를 담당하는 서버 부분입니다. (Python/FastAPI)
- 두 시스템은 독립적으로 실행되며, API를 통해 데이터를 주고받는 방식으로 통신합니다.

---

## 3. 프론트엔드 상세 (`frontend` 폴더)
- **핵심 프레임워크:** **React.js** (Vite 기반으로 구축하여 빠르고 효율적입니다.)
- **사용 언어:** JavaScript (JSX)
- **스타일링:** **Bootstrap**을 사용하여 반응형 및 모던한 UI를 구현했습니다.
- **주요 라이브러리:**
  - `axios`: 백엔드 API와 통신(HTTP 요청)하는 역할을 합니다.
- **주요 기능:**
  - 갤러리에 이미지를 추가하는 기능.
  - 유사도를 검색할 이미지를 업로드하는 기능.
  - 검색에 사용한 이미지와 그 결과를 함께 보여주는 기능.
  - 갤러리에 저장된 모든 이미지 목록을 확인하는 기능.

---

## 4. 백엔드 상세 (`backend` 폴더)
- **핵심 프레임워크:** **FastAPI** (Python 기반의 고성능 웹 프레임워크입니다.)
- **사용 언어:** Python
- **이미지 인식 모델:**
  - **OpenCLIP ViT-L-14** (`datacomp_xl_s13b_b90k` 사전학습 가중치 사용)
  - 수십억 개의 이미지-텍스트 쌍으로 학습된 강력한 AI 모델입니다.
  - 이미지의 시각적 특징과 맥락을 이해하여 고차원 **벡터(Vector)**로 변환하는 핵심적인 역할을 합니다.
- **주요 라이브러리:**
  - `open-clip-torch`, `torch`: OpenCLIP 모델을 로드하고 실행합니다.
  - `Pillow`: 이미지 파일을 열고 기본적인 처리를 합니다.
  - `scikit-learn` (`numpy`): 이미지 벡터 간의 **코사인 유사도**를 계산하여 얼마나 비슷한지를 측정합니다.
  - `uvicorn`: FastAPI 애플리케이션을 실행하는 웹 서버입니다.
- **주요 API 엔드포인트:**
  - `POST /upload`: 이미지를 받아 저장하고, AI 모델로 벡터를 추출하여 관리합니다.
  - `POST /search`: 검색 이미지를 받아 벡터를 추출하고, 저장된 모든 벡터와 유사도를 비교하여 상위 5개 결과를 반환합니다.
  - `GET /gallery`: 현재 갤러리에 있는 모든 이미지의 파일명 목록을 반환합니다.
  - `GET /storage/...`: 업로드된 이미지 파일을 직접 제공합니다.

---

## 5. 데이터 저장소 (`storage` 폴더)
- **이미지 파일:** 업로드된 원본 이미지들은 `storage/images` 폴더에 파일 형태로 직접 저장됩니다.
- **벡터 데이터:** 각 이미지의 파일명과 AI 모델이 추출한 벡터 값은 `storage/vectors.json` 파일에 JSON 형태로 저장됩니다.
